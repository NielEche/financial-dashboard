import React from 'react';

const Loading = () => {
    return (
        <div className="absolute inset-0 flex items-center bg-gray-200 justify-center">
            <div className='font-black text-2xl'>Loading...</div>
        </div>
    );
};

export default Loading;
